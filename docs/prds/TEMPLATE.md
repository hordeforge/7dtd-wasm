# PRD: <capability>

## Status

Draft / In progress / Implemented. Name the modules that are the source of
truth and the surfaces that expose it (the host API, a console command, a
gate). If part of this page is already stale, say so here rather than
leaving a reader to discover it.

## Problem

What is impossible or unsafe without this, stated from the situation rather
than from the solution. Include the real constraints that shape it: guests
are hostile, limits are enforced by the host never by guest goodwill, the
in-game bridge is net48, the server tick is 50 ms.

## Goals

Numbered and verifiable; each one matches a box under Acceptance.

## Non-goals

What this deliberately does not do, and why.

## Acceptance

Checkboxes that match the Goals. A goal is only done when its box is
checked by evidence: a passing test, a verified game target, an acceptance
run, a doc gate.
