[package]
name = "guest-bigmem"
version = "0.1.0"
edition = "2021"

[lib]
name = "guest_bigmem"
crate-type = ["cdylib"]

[dependencies]
guest-common = { path = "../../guest-common" }

[lints]
workspace = true
